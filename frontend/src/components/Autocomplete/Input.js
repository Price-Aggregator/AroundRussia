/* import React from 'react';

export default function Input(props) {
	// eslint-disable-next-line react/prop-types
	const { onChange, input, classInput, name, placeholder } = props;
	return (
		<input
			type="text"
			onChange={onChange}
			value={input}
			className={classInput}
			placeholder={placeholder}
			name={name}
			required
		/>
	);
} */
